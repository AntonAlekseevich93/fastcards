meta:
  environment: jvm
  toolchain:
    name: gradle
    version: 11

build:
  args: -Dserver.port=80
  artifacts:
    "build/libs/*.jar": /

run:
  containerPort: 8080
  jarName: com.fastcards.fastcards.backend-0.0.1.jar
